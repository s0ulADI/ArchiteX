export default function GeneratedComponent({ sections }) {
  return (
    <div className="mx-auto flex max-w-6xl flex-col gap-6 px-6 py-8">
      {sections.map((section) => (
        <section key={section.id} className="rounded border border-slate-200 bg-white p-5">
          <p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{section.type}</p>
          <h2 className="mt-2 text-xl font-semibold">{section.label}</h2>
          <p className="mt-2 text-sm text-slate-600">{section.description}</p>
        </section>
      ))}
    </div>
  );
}
